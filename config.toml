[paths]
artifacts_dir = "artifacts"
reports_dir   = "reports"

[sandbox]
image        = "pkgids-sandbox:latest"
timeout_secs = 120
network      = "none"

[scoring]
malicious_threshold  = 0.8
suspicious_threshold = 0.5

[logging]
level = "INFO"
